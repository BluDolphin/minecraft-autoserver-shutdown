# minecraft-autoserver-shutdown
A simple python script to query minecraft server running locally, and if no players are online for two consecutive checks, terminate the server tmux and shutdown the system
